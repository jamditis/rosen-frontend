name: Frontend Validation

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/**'
      - 'faq/**'
      - 'tools/**'
      - 'dissertation/**'
      - 'dissertation-launch/**'
      - 'docs/**'
      - 'features/**'
      - 'index.html'
      - 'metadata.json'
      - 'version.json'
      - 'shared-styles.css'
      - '.htaccess'
      - 'ADDING-RECORDS.md'
      - 'tests/**'
      - 'scripts/**'
      - 'wiki/**'
      - 'data/**'
      - 'backend/entity_extraction_schema.json'
      - 'backend/entity_extraction_schema_v3.json'
      - 'preservation/**'
      - 'workers/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/frontend-validation.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'frontend/**'
      - 'faq/**'
      - 'tools/**'
      - 'dissertation/**'
      - 'dissertation-launch/**'
      - 'docs/**'
      - 'features/**'
      - 'index.html'
      - 'metadata.json'
      - 'version.json'
      - 'shared-styles.css'
      - '.htaccess'
      - 'ADDING-RECORDS.md'
      - 'tests/**'
      - 'scripts/**'
      - 'wiki/**'
      - 'data/**'
      - 'backend/entity_extraction_schema.json'
      - 'backend/entity_extraction_schema_v3.json'
      - 'preservation/**'
      - 'workers/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/frontend-validation.yml'

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # playwright is a repo dependency; its postinstall downloads ~hundreds of
    # MB of browser binaries that this validation job never uses.
    env:
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: '1'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          # The stewardship census stamps the last commit that changed any
          # input file, so its freshness test needs ancestor history.
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check JavaScript syntax
        run: |
          echo "Checking JavaScript files with node --check..."
          status=0
          while IFS= read -r file; do
            if node --check "$file"; then
              echo "ok: $file"
            else
              echo "::error file=$file::JavaScript syntax error"
              status=1
            fi
          done < <(find . -name '*.js' \
            -not -path './node_modules/*' \
            -not -path './.git/*' \
            -not -path './backend/*' \
            -not -path './archived/*')
          exit $status

      - name: Verify main entry points exist
        run: |
          echo "Verifying main entry points..."
          for f in index.html frontend/index.js frontend/App.js frontend/constants.js; do
            if [ ! -f "$f" ]; then
              echo "::error::Missing required file: $f"
              exit 1
            fi
            echo "ok: $f"
          done
          for d in frontend/components frontend/services; do
            if [ ! -d "$d" ]; then
              echo "::error::Missing required directory: $d"
              exit 1
            fi
            echo "ok: $d/"
          done

      - name: Run frontend test suite
        run: npm run test:frontend

      - name: Run source-discovery Worker tests
        run: npm run test:workers

      - name: Run data integrity test suite
        run: npm run test:data

      - name: Run extraction coverage check
        run: npm run test:data:extraction-coverage

      - name: Validate OKF bundle
        run: npm run test:okf

      - name: Check HTML documents are well-formed
        run: |
          echo "Checking HTML files..."
          for file in $(find . -name '*.html' -not -path './node_modules/*' -not -path './.git/*' -not -path './backend/*'); do
            if ! grep -q '<html' "$file" && ! grep -q '<!DOCTYPE' "$file"; then
              echo "Warning: $file may not be a complete HTML document"
            fi
          done

      - name: List external CDN references
        run: |
          echo "External CDN references in HTML files:"
          for file in $(find . -name '*.html' -not -path './node_modules/*' -not -path './.git/*' -not -path './backend/*'); do
            grep -oE 'https?://[^"'\''[:space:]]*' "$file" 2>/dev/null \
              | grep -E '(esm\.sh|unpkg\.com|jsdelivr\.net|cdnjs\.cloudflare\.com|cdn\.)' | sed "s|^|  $file: |" || true
          done

      - name: Check for TODO and FIXME comments
        run: |
          echo "Scanning for TODO/FIXME comments..."
          grep -rn 'TODO\|FIXME' --include='*.js' --include='*.html' \
            --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=backend --exclude-dir=archived \
            | head -20 || echo "No TODO/FIXME found"
        continue-on-error: true
